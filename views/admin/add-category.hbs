<section>
    <div class="container mt-4">
     <div class="row">
         <div class="col-md-6">
             <h2 class="text-center">Add category</h2>
             <form action="/admin/add-category" method="post" enctype="multipart/form-data">
                 <label for="">Category Name</label>
                 <input type="text" name="category_name" class="form-control" id="name">
                 <span id="errorName" style="color:crimson"></span>
                 
                 
               
                 <button type="Submit" class="btn btn-success mt-4">Submit</button>

             </form>
         </div>
     </div>
    </div>
    
</section>

<script>

let form=document.getElementById('form')
let category=document.getElementById('name')

let errorCatName=document.getElementById('errorName')

form.addEventListener('submit',(e)=>{
     let nameMessage=[]

         if(name.value==''||name.value==null){
			 nameMessage.push('Enter the Name')
		 }else if(isNaN(fname.value)==false){
			 nameMessage.push('Numbers are Not Allowed')
		 }

         if(nameMessage.length>0){
			 e.preventDefault()
			errorCatName.innerHTML=nameMessage.join('')
		 }
})
</script>